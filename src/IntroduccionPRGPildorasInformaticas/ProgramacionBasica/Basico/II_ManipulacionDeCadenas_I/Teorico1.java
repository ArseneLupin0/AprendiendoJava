package IntroduccionPRGPildorasInformaticas.ProgramacionBasica.Basico.II_ManipulacionDeCadenas_I;

public class Teorico1 {

//#############################################################################
//# Clase String I - Píldoras Informáticas - Video 11                         #
//#############################################################################

    /* --> Clase String
     *
     * Propiedades:
     *  - String no es un tipo primitivo.
     *
     * ¿Cómo almacenar una cadena de caracteres?
     *  - String mi_nombre = "Juan"; donde mi_nombre es un objeto (instancia, ejemplar) de la clase String.
     *
     * Métodos (más usados) de la clase String para manipulación de cadenas de texto:
     *  - length(): devuelve la longitud de una cadena de caracteres.
     *
     *  - chartAt(n): devuelve la posición de un carácter dentro de una cadena (empieza desde 0).
     *
     *  - substring(x,y): devuelve una sub-cadena dentro de la cadena, siendo X el carácter a partir
     *                    del cuál se extrae e Y el nº de caracteres que se quieren extraer.
     *
     *  - equals(cadena): devuelve true, si dos cadenas que se comparan son iguales y false si no lo son.
     *                    Distingue entre mayúsculas y minúsculas.
     *
     *  - equalsIgnoreCase(cadena): lo mismo que la anterior pero ignorando las mayúsculas y minúsculas.
     *
     *
     */
}
